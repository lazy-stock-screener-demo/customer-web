export const readCatalogByTickerLogic: any = (reqDTO) => {
  // const schema = Joi.object({
  //   username: Joi.string().alphanum().min(3).max(30).required(),
  // });
  // const { error, value } = schema.validate({ a: 'a string' });
  // throw error
  return true;
};
